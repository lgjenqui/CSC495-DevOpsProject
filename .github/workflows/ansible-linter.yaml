name: ansible linter

on:
  pull_request:
    branches:
      - dev

jobs:
  build:
    name: Ansible Lint
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run ansible-lint
        run: |
          docker run --rm -v $(pwd):/playbook ansible/ansible-lint ansible-playbook /playbook/ansible-linter.yaml
